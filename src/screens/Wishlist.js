import React from 'react';

import {View, Text} from 'react-native';

const Wishlist = ({navigation}) => {
    return(
        <View>
            <Text style={{marginTop: 200}}>This is the Wishlist screen</Text>
        </View>
    )
}

export default Wishlist;